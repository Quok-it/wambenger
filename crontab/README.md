This is a cron job for Quok single-node audits

This cron job runs using the crond daemon on linux systems

Just run the following to install the quok package and schedule the cronjob

```
sudo bash quokcron.sh <API KEY> <PROVIDERNAME>
```
